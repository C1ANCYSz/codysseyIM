{
  "name": "codyssey",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "cd backend && nodemon server.js",
    "frontend": "cd front-end && npm run dev",
    "all": "concurrently \"npm run backend\" \"npm run frontend\"",
    "install": "cd front-end && npm install && npm run build && cd ../backend && npm install",
    "git": "git pull origin main",
    "start": "npm run install && npm run all",
    "startPro": "npm run install && concurrently \"cd backend && node server.js\" \"cd front-end && npm run dev\""

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "nodemon": "^3.1.10"
  }
}
