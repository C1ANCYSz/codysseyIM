{
  "name": "codyssey",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "backend": "cd backend && nodemon server.js",
    "frontend": "cd front-end && npm run dev",
    "all": "concurrently \"npm run backend\" \"npm run frontend\"",
    "install": "cd front-end && npm install && cd ../backend && npm install",
    "git":"git pull origin main",
    "start":"npm run install && npm run all"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
